var _ = require("lodash");
// Load the core build.

var array = [1, 2, 3, 4, 5, 6, 7, 8];
console.log("answer: ", _.without(array, 3));

console.log(_);
const object = {
  name: "Sally",
};

object.sally;
